package com.codegym.sort.comparator;

import com.codegym.sort.Animal;

public class Dog extends Animal {
    public Dog(String name, boolean isAlive, int weight) {
        super(name, isAlive, weight);
    }
}
